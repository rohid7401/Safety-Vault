namespace PasswordManager
{
    internal static class Program
    {
        [STAThread]
        static void Main()
        {
            Console.WriteLine("¡Bienvenido a PasswordManager!");

            //ApplicationConfiguration.Initialize();
            //Application.Run(new Form1());
        }
    }
}